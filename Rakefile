# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/osx'

Motion::Project::App.setup do |app|
  app.name = 'TestingObjCFromRubyMotion'
  app.files = []
  app.vendor_project('TestingObjCFromRubyMotion', :xcode, :target => 'StaticLib', :xcodeproj => "../TestingObjCFromRubyMotion.xcodeproj", :force_rebuild => true)
end
